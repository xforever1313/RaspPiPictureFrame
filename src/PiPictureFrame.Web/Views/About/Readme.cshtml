@{
    ViewData["Title"] = "Readme";
}

@using MarkdownSharp;
@model PiPictureFrame.Api.Resources;

@{
    var markdown = new MarkdownSharp.Markdown();
    string html = markdown.Transform( Model.GetReadme() );
}

<div class="content">
    @Html.Raw( html )
</div>
